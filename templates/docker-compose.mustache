services:
{{#services}}
    {{{name}}}:
        build: ./{{{path}}}
        platform: linux/amd64
    {{#has_ports}}
        ports:
    {{/has_ports}}
    {{#ports}}
            - {{{external}}}:{{{internal}}}
    {{/ports}}
{{/services}}
